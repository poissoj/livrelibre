/** @type {import("next").NextConfig} */
const nextConfig = {
  i18n: {
    locales: ["fr"],
    defaultLocale: "fr",
  },
  reactStrictMode: true,
  poweredByHeader: false,
};

module.exports = nextConfig;
