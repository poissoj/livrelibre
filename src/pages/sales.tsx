import { Title } from "@/components/Title";

const Sales = (): JSX.Element => {
  return (
    <div>
      <Title>Liste des ventes par mois</Title>
      Liste des ventes par mois
    </div>
  );
};

export default Sales;
