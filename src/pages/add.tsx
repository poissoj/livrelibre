import { Title } from "@/components/Title";

const Add = (): JSX.Element => {
  return (
    <div>
      <Title>Ajouter un article</Title>
      Ajouter un article
    </div>
  );
};

export default Add;
